## This Machine (hard limits, never override)

No declared hard limits for this host yet. Before heavy compute or a source
build, say so in one line. Create
`bootstrap/global/hosts/<LocalHostName>.md` in the hub (or your private
overlay) to declare limits.
